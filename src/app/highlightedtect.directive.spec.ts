import { HighlightedtectDirective } from './highlightedtect.directive';

describe('HighlightedtectDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightedtectDirective();
    expect(directive).toBeTruthy();
  });
});
